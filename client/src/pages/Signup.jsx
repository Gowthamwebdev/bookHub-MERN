import SignupUi from "../Ui/SignupUi";

const Signup = () => {
  return <SignupUi />;
};

export default Signup;

/*const isValidPassword = (password) => /^(?=.*[A-Z])(?=.*[a-z])(?=.*\d)(?=.*[!@#$%^&*()_+{}\[\]:;<>,.?~]).{8,}$/.test(password);
const isValidEmail = (email) => /^[\w.-]+@[a-zA-Z\d.-]+\.[a-zA-Z]{2,}$/.test(email);
*/
